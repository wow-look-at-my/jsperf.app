{
  "name": "jsperf-standalone",
  "version": "0.0.0",
  "private": true,
  "description": "Self-contained single-file jsPerf builds (localStorage app + kiosk) and the jsperf-pack CLI",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "smoke": "node scripts/browser-smoke.mjs",
    "check": "npm run build && npm run verify && npm run smoke"
  },
  "devDependencies": {
    "playwright": "1.56.0"
  },
  "dependencies": {
    "lodash": "4.17.21"
  }
}
